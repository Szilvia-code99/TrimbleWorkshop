// used to include the System namespace
using Curs1;
using System; 

// Program structure: namespace, class, class methods, a main method, statements, comments

// namespace (collection of classes) declaration
namespace App1 
{
    // class declaration, contains data and method(s) definitions
    // methods define the behavior of the class
    class Program
    {

        //Main method: entry point for all C# programs
       /*private static void Main(string[] args)
        {
            //WriteLine is a method of the Console class defined in the System namespace 
            Console.WriteLine("C# Workshop Introduction");
            
            //Add this line to prevent the console from closing
            Console.Read();

            //instantiating the BasicSyntaxWindow class
            BasicSyntaxWindow window = new BasicSyntaxWindow();
            window.SetDimensions();
            window.Display();
            Console.ReadKey();
        }*/
    }
}
